# Quickstart: caption a clip

Runs the offline default pipeline on a synthetic clip and prints a caption.

```bash
python run.py
```

No media files or model weights required — the clip is generated in-process and
the `TemplateLLM` produces the sentence deterministically.
